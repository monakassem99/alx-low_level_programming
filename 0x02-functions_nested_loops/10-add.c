#include "main.h"

/**
 *add - prints the summation of two integers
 *@a: is the first int to be computed
 *@b: is the second int to be computed
 *
 * Return: the result
 */

int add(int a, int b)
{
	return (a + b);
}
